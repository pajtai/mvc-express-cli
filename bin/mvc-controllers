#!/usr/bin/env node

'use strict';

const program = require('commander');

program
    .command('list', 'show all controllers')
    .command('new <name> [type]', 'create a new controller')
    .parse(process.argv);
